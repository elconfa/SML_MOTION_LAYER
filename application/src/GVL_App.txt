(* ============================================================
   GVL_App — APPLICATION configuration (machine-dependent)
   SML — application level

   Constants owned by the machine project, NOT by the SML library.
   The number of axes lives here: change only this to scale the
   GVL_AXIS arrays and the MAIN loops.

   Qualified reference: GVL_App.MAX_AXIS
   ============================================================ *)

{attribute 'qualified_only'}
VAR_GLOBAL CONSTANT
    MAX_AXIS : UINT := 4;     // number of axes handled by MAIN / array size
END_VAR
