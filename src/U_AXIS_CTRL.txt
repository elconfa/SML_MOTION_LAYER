(* ============================================================
   U_AXIS_CTRL — UNION bus per il comando asse
   SML v8 — Livello B / MAPPING (nuovo)

   Overlay dello stesso blocco di memoria come:
     - stData : ST_AXIS_CTRL   (vista tipizzata per la logica)
     - aRaw   : blocco byte     (vista grezza per il mapping fieldbus)

   Dimensione fissa = MAP_SIZE_CTRL (>= SIZEOF(ST_AXIS_CTRL), verificato
   da MAPPING_in). Consente di linkare l'elemento al process image come
   struct o come blocco grezzo senza cambiare la dimensione mappata.
   ============================================================ *)

TYPE U_AXIS_CTRL :
UNION
    stData : ST_AXIS_CTRL;
    aRaw   : ARRAY[1..GVL_SML_CONST.MAP_SIZE_CTRL] OF BYTE;
END_UNION
END_TYPE
